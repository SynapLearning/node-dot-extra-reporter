{
  "name": "node-dot-extra-reporter",
  "version": "1.0.0",
  "description": "",
  "private": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./build/dot-extra-reporter.d.ts",
      "default": "./build/dot-extra-reporter.js"
    }
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "rm -rf build && npm run-script build:src",
    "build:src": "tsc --p tsconfig.json",
    "test": "tsc && node --test-reporter=./build/dot-extra-reporter.js --test"
  },
  "dependencies": {
    "chalk": "5.3.0"
  },
  "devDependencies": {
    "@types/node": "20.10.0",
    "ts-node": "10.9.1",
    "typescript": "5.3.2"
  },
  "keywords": [
    "node",
    "test",
    "reporter",
    "dot",
    "spec"
  ],
  "author": "",
  "license": "MIT"
}
